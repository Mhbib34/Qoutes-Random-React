export default function RepeatButton({ onClick }) {
  return (
    <i
      className="bx bx-repeat py-1.5 text-lg text-white cursor-pointer rounded-md px-3 bg-primary"
      onClick={onClick}
    ></i>
  );
}
